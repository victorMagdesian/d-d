// data/campaignStore.js

// Objeto que armazenará todas as campanhas
// Chave: id da campanha, Valor: dados da campanha
if (!global.campaigns) {
    global.campaigns = {};
  }
  
  const campaigns = global.campaigns;
  
  export default campaigns;